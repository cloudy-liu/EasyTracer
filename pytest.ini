[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    real_device: requires connected Android device
    slow: test duration > 10 seconds
    gui: requires display for PySide6
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
