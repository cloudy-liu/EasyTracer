[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "easy_tracer"
version = "0.1.0"
description = "A GUI for various performance tracing tools"
requires-python = ">=3.10"
dependencies = [
    "PySide6",
]

[project.scripts]
easytracer = "easy_tracer.main:run"

[project.optional-dependencies]
dev = [
    "ruff",
    "black",
    "mypy",
    "pytest",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["easy_tracer*"]

[tool.setuptools.package-data]
# Ensure vendored systrace HTML assets are available when installed as a package.
"easy_tracer.framework.external.systrace" = ["*.html"]

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.mypy]
python_version = "3.10"
strict = true
ignore_missing_imports = true
